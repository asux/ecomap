!!!
%html
  %head
    %title Ecomap
    %meta{:charset => "utf-8"}
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    %section
      %header
      - unless flash.empty?
        #flash-container
          - flash.each do |type, msg|
            %p{:class => "#{type}"}= msg
      %article#content= yield
      %footer