!!!
%html
  %head
    %title= @page_title + " : Ecomap"
    %meta{:charset => "utf-8"}
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    %section#container
      %nav.main
        %menu
          %li= link_to t('title.main', :default => "Main"), '/'
      %aside
        = render 'users/user_nav'
      %header
        - unless flash.empty?
          #flash-container
            - flash.each do |type, msg|
              %p{:class => "#{type}"}= msg
        - if @title
          %h1= @title
      %article#content= yield
      %footer
        = t('footer.created', :default => 'Created by')
        = mail_to raw(t('footer.email', :default => "Alexander Ulyanitsky <a.ulyanitsky@gmail.com>")),
                  t('footer.author', :default => "Alexander Ulyanitsky"), :encode => :hex
        &copy; 2011
