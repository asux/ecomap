- title t('devise.unlock', :default => "Resend unlock instructions")
= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f|
  = devise_error_messages!
  %p
    = f.label :email
    %br/
    = f.text_field :email
  %p= f.submit t('devise.unlock', :default => "Resend unlock instructions")
= render :partial => "devise/shared/links"
