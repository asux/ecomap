- semantic_form_for(@user) do |f|
  - f.inputs do
    = f.input :email
    -# f.input :password
    -# f.input :password_confirmation
    -# f.input :remember_me
    = f.input :full_name
    = f.input :info
    - if can? :assign_role, @user
      = f.input :role, :as => :radio, :collection => User::ROLES_INSTANCES
  - f.buttons do
    = f.commit_button 
